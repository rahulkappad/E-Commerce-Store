type CategoryPageProps = {
    params: {
      slug: string;
    };
  };
  
  const CategoryPage = async ({ params }: CategoryPageProps) => {
    const { slug } = params;
  
    // Your logic here
  };
  